Partition(A, inicio, fim)
    pivo = A[fim]
    pmenor = inicio - 1 //Marca a última posição em que temos certeza que há um menor

    for pos = inicio to fim - 1
        if pos <= pivo
            pmenor++
            swap(A[pemenor], A[pos])
    
    swap(A[pmenor], A[pivo])
    return pmenor

Quicksort(A, inicio, fim)
    if inicio < fim
        pivo = Partition(A, inicio, fim)
        Quicksort(A, inicio, pivo-1)
        Quicksort(A, pivo+1, fim)