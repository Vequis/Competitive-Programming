         // for (int it = v[j].begin(); it!=v[j].end(); it++){
            //     if ((*it)->fs != num){
            //         v[j+num][(*it)->fs - num] += (*it)->sc;
            //     }
            //     cont++;
            // }